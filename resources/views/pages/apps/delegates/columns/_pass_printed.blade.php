<!--begin::User details-->
<div class="d-flex">
    <span class="badge badge-{{ $delegate->pass_printed ? "success":'danger' }}">{{ $delegate->pass_printed ? "Printed":'Pending' }}</span>
</div>
<!--begin::User details-->
